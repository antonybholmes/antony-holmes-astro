---
import type { IClassProps } from '@/interfaces/class-props'
import { getReadTimeUrl } from '@/lib/post/post'
import { Clock } from 'lucide-react'
import { VCenterRow } from './layout/v-center-row'

interface Props extends IClassProps {
  readingTime: number
}

const { readingTime } = Astro.props
---

<VCenterRow className="gap-x-2">
  <Clock
    data-mode="light"
    className="stroke-foreground/40 w-5 data-[mode=dark]:stroke-white"
    strokeWidth={1.5}
  />
  <a href={getReadTimeUrl(readingTime)} class="hover:underline">
    {readingTime} min{readingTime > 1 ? 's' : ''} read
  </a>
</VCenterRow>
